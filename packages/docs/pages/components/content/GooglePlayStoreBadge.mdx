import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { GooglePlayStoreBadge } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/GooglePlayStoreBadge.code';

Alt tags should always be translated as they are requried for screen readers.

<LiveEditorBlock code={code} scope={{ GooglePlayStoreBadge }} />
<GeneratePropsTable componentName="GooglePlayStoreBadge" />

export const meta = {
  name: 'GooglePlayStoreBadge',
};

;
