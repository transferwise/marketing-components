import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { AppStoreBadge } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/AppStoreBadge.code';

We have 2 different urls for App Store. One for GB(this one is used for every other `locale` as well) and one for US.
Alt tags should always be translated as they are requried for screen readers.

<LiveEditorBlock code={code} scope={{ AppStoreBadge }} />
<GeneratePropsTable componentName="AppStoreBadge" />

export const meta = {
  name: 'AppStoreBadge',
};

;
