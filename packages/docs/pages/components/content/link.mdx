import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Link } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/Link.code';

<LiveEditorBlock code={code} scope={{ Link }} />
<GeneratePropsTable componentName="Link" />

export const meta = {
  name: 'Link',
};
