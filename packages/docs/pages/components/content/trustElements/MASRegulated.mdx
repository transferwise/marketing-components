import { LiveEditorBlock, GeneratePropsTable } from '../../../../utils';
import { MASRegulated } from '@transferwise/marketing-components';
import code from '../../../../liveEditorCode/trustElements/MASRegulated.code';

<LiveEditorBlock code={code} scope={{ MASRegulated }} />
<GeneratePropsTable componentName="MASRegulated" />

export const meta = {
  name: 'MASRegulated',
};