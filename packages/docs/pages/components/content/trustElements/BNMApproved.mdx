import { LiveEditorBlock, GeneratePropsTable } from '../../../../utils';
import { BNMApproved } from '@transferwise/marketing-components';
import code from '../../../../liveEditorCode/trustElements/BNMApproved.code';

<LiveEditorBlock code={code} scope={{ BNMApproved }} />
<GeneratePropsTable componentName="BNMApproved" />

export const meta = {
  name: 'BNMApproved',
};