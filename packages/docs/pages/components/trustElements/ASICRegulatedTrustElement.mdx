import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { ASICRegulatedTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/ASICRegulatedTrustElement.code';

<LiveEditorBlock code={code} scope={{ ASICRegulatedTrustElement }} />
<GeneratePropsTable componentName="ASICRegulatedTrustElement" />

export const meta = {
  name: 'ASICRegulatedTrustElement',
};
