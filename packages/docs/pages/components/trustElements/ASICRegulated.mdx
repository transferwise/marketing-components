import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { ASICRegulated } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/ASICRegulated.code';

<LiveEditorBlock code={code} scope={{ ASICRegulated }} />
<GeneratePropsTable componentName="ASICRegulated" />

export const meta = {
  name: 'ASICRegulated',
};
