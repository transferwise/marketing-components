import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { CAndEDRegulated } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/CAndEDRegulated.code';

<LiveEditorBlock code={code} scope={{ CAndEDRegulated }} />
<GeneratePropsTable componentName="CAndEDRegulated" />

export const meta = {
  name: 'CAndEDRegulated',
};
