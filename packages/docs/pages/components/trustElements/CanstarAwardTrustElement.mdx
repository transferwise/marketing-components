import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { CanstarAwardTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/CanstarAwardTrustElement.code';

<LiveEditorBlock code={code} scope={{ CanstarAwardTrustElement }} />
<GeneratePropsTable componentName="CanstarAwardTrustElement" />

export const meta = {
  name: 'CanstarAwardTrustElement',
};
