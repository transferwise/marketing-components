import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { FSRAApprovedTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/FSRAApprovedTrustElement.code';

<LiveEditorBlock code={code} scope={{ FSRAApprovedTrustElement }} />
<GeneratePropsTable componentName="FSRAApprovedTrustElement" />

export const meta = {
  name: 'FSRAApprovedTrustElement',
};
TrustElemen;

t
