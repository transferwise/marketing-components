import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { JPFSARegulated } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/JPFSARegulated.code';

<LiveEditorBlock code={code} scope={{ JPFSARegulated }} />
<GeneratePropsTable componentName="JPFSARegulated" />

export const meta = {
  name: 'JPFSARegulated',
};

;
