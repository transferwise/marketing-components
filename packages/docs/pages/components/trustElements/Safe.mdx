import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Safe } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/Safe.code';

<LiveEditorBlock code={code} scope={{ Safe }} />
<GeneratePropsTable componentName="Safe" />

export const meta = {
  name: 'Safe',
};

;
