import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { MASRegulatedTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/MASRegulatedTrustElement.code';

<LiveEditorBlock code={code} scope={{ MASRegulatedTrustElement }} />
<GeneratePropsTable componentName="MASRegulatedTrustElement" />

export const meta = {
  name: 'MASRegulatedTrustElement',
};
