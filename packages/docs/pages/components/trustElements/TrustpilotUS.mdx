import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { TrustpilotUS } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/TrustpilotUS.code';

<LiveEditorBlock code={code} scope={{ TrustpilotUS }} />
<GeneratePropsTable componentName="TrustpilotUS" />

export const meta = {
  name: 'TrustpilotUS',
};
