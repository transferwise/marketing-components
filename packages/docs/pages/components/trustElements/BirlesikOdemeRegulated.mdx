import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { BirlesikOdemeRegulated } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/BirlesikOdemeRegulated.code';

<LiveEditorBlock code={code} scope={{ BirlesikOdemeRegulated }} />
<GeneratePropsTable componentName="BirlesikOdemeRegulated" />

export const meta = {
  name: 'BirlesikOdemeRegulated',
};

;
