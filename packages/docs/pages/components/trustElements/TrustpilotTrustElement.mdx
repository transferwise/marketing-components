import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { TrustpilotTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/TrustpilotTrustElement.code';

<LiveEditorBlock code={code} scope={{ TrustpilotTrustElement }} />
<GeneratePropsTable componentName="TrustpilotTrustElement" />

export const meta = {
  name: 'TrustpilotTrustElement',
};
