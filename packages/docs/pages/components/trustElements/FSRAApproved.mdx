import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { FSRAApproved } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/FSRAApproved.code';

<LiveEditorBlock code={code} scope={{ FSRAApproved }} />
<GeneratePropsTable componentName="FSRAApproved" />

export const meta = {
  name: 'FSRAApproved',
};

;
