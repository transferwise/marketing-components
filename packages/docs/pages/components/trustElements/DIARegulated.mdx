import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { DIARegulated } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/DIARegulated.code';

<LiveEditorBlock code={code} scope={{ DIARegulated }} />
<GeneratePropsTable componentName="DIARegulated" />

export const meta = {
  name: 'DIARegulated',
};

;
