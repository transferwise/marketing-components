import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { FINTRACRegulated } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/FINTRACRegulated.code';

<LiveEditorBlock code={code} scope={{ FINTRACRegulated }} />
<GeneratePropsTable componentName="FINTRACRegulated" />

export const meta = {
  name: 'FINTRACRegulated',
};
