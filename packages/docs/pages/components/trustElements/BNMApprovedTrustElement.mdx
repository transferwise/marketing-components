import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { BNMApprovedTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/BNMApprovedTrustElement.code';

<LiveEditorBlock code={code} scope={{ BNMApprovedTrustElement }} />
<GeneratePropsTable componentName="BNMApprovedTrustElement" />

export const meta = {
  name: 'BNMApprovedTrustElement',
};
