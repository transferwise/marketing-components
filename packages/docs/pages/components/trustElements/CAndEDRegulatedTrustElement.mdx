import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { CAndEDRegulatedTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/CAndEDRegulatedTrustElement.code';

<LiveEditorBlock code={code} scope={{ CAndEDRegulatedTrustElement }} />
<GeneratePropsTable componentName="CAndEDRegulatedTrustElement" />

export const meta = {
  name: 'CAndEDRegulatedTrustElement',
};
