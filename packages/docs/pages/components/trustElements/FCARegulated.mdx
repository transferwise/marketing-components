import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { FCARegulated } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/FCARegulated.code';

<LiveEditorBlock code={code} scope={{ FCARegulated }} />
<GeneratePropsTable componentName="FCARegulated" />

export const meta = {
  name: 'FCARegulated',
};

;
