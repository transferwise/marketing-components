import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { FCARegulatedTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/FCARegulatedTrustElement.code';

<LiveEditorBlock code={code} scope={{ FCARegulatedTrustElement }} />
<GeneratePropsTable componentName="FCARegulatedTrustElement" />

export const meta = {
  name: 'FCARegulatedTrustElement',
};
