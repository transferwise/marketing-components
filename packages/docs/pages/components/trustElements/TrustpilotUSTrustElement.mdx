import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { TrustpilotUSTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/TrustpilotUSTrustElement.code';

<LiveEditorBlock code={code} scope={{ TrustpilotUSTrustElement }} />
<GeneratePropsTable componentName="TrustpilotUSTrustElement" />

export const meta = {
  name: 'TrustpilotUSTrustElement',
};
