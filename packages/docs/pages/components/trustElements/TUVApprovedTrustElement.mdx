import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { TUVApprovedTrustElement } from '@transferwise/marketing-components';
import code from '../../../liveEditorCode/trustElements/TUVApprovedTrustElement.code';

<LiveEditorBlock code={code} scope={{ TUVApprovedTrustElement }} />
<GeneratePropsTable componentName="TUVApprovedTrustElement" />

export const meta = {
  name: 'TUVApprovedTrustElement',
};
